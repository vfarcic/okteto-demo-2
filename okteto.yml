name: okteto-demo-2
build:
  silly-demo:
    context: .
    dockerfile: Dockerfile
deploy:
  - kubectl apply -f k8s
dev:
  silly-demo:
    image: vfarcic/silly-demo:latest
    command: bash
    workdir: /src
    sync:
      - .:/src
    environment:
      - name=$USER
    forward:
      - 8080:8080

